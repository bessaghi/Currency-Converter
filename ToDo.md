# To Do

## Improvements
- Fix fixedRate that gets undefined (I created the bug at the end of the development and didn't get time to fix it up :/ )
- Fix the values not always being limited to only 2 digits
- UX: 
  - Add more colors
  - Make it responsive
  - Fix the table length to match the convert card
- Add message when rate > 2% of current rate
- Add more tests
